
/**
 * display string on the STDOUT
 * @param {*} str string parameter to be printed stdout
 * @author Kaycodes
 */
function displayMessage(str) {
    console.log(str);
}
module.exports = displayMessage;
